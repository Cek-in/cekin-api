export enum UserType {
  DEV = "DEVELOPER",
  SUPERADMIN = "SUPER_ADMIN",
  ADMIN = "ADMIN",
  USER = "USER",
}

export enum PotentialCovid {
  SEVERE = "SEVERE",
  MODERATE = "MODERATE",
  LOW = "LOW",
  NONE = "NONE",
}

export enum OwnCheckInDay {
  TODAY = "TODAY",
  YESTERDAY = "YESTERDAY",
  OLDER = "OLDER",
}
